//allocate create new init set get copy delete : json

Lib

typedef struct { byte buf [ 2048 ] ; } Buffer ;
/*
cOn
void 
htmlOpenX ( thtml | buffer, Buffer buffer0 )
{
    buffer = &buffer0.buf ;
    memset ( buffer, 0, Buffer.size ) ; 
    sprintf ( buffer, "xdg-open %s", thtml ) ;
    printf ( "\n%s\n", buffer ) ;
    //_sh ( buffer ) ;
}
htmlOpenX ( "hi" ) ;
pause
cOff
*/
